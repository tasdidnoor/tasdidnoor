# Project Overview & Git Milestones

## Overview
- **Purpose:** A dedicated repository for biographical content and specific project snapshots.
- **Git Linkage:** Connected to the remote repository at `https://github.com/tasdidnoor/tasdidnoor.git`.

## Technical Setup
- **Branching:** Local branch aligned to `main` for GitHub compatibility.
- **Upstream:** Configured using `git push -u origin main`.
- **Submodule Handling:** Best practice to avoid nested `.git` folders for clean repository structure.

## Git Learning Milestones
- Mastered the core workflow: `status` -> `add` -> `commit` -> `push`.
- Handled "unrelated histories" using `--allow-unrelated-histories` for complex merges.
- Managed remote connections using `remote add` and `remote set-url`.
